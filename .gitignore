.venv/
__pycache__/
*.pyc
.pytest_cache/

# training artefacts: SB3 checkpoints are ~56MB each (they carry optimiser state)
runs/

# the served policy is tracked; training checkpoints are not
!models/*.onnx

.DS_Store
.playwright-mcp/

CLAUDE.md
.claude/
